<header class="site-header">
  <div class="nav-container">
    <a href="/" class="logo" aria-label="Mitchell Haw home">
      <span class="logo-mark">MH</span>
      <span class="logo-copy">
        <strong>Mitchell Haw</strong>
        <small>SEO for Bend + Central Oregon</small>
      </span>
    </a>

    <nav class="main-nav" aria-label="Primary navigation">
      <a href="/services/seo/">SEO</a>
      <a href="/services/ai-search/">AI Search</a>
      <a href="/services/seo-bend-oregon/">Local SEO</a>
      <a href="/insights/">Insights</a>
      <a href="/about/">About</a>
      <a href="/contact/" class="nav-cta">Let’s Talk</a>
    </nav>
  </div>
</header>

<style is:global>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 999;
    background: rgba(246, 244, 238, 0.95);
    border-bottom: 1px solid #d9ddd7;
    backdrop-filter: blur(14px);
  }

  .site-header .nav-container {
    width: min(1180px, 92%);
    min-height: 80px;
    margin: 0 auto;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 30px;
    background: transparent;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    backdrop-filter: none;
  }

  .site-header .logo {
    display: inline-flex;
    align-items: center;
    gap: 11px;
    color: #1c2923;
    text-decoration: none;
  }

  .site-header .logo-mark {
    display: inline-grid;
    place-items: center;
    width: 38px;
    height: 38px;
    flex: 0 0 38px;
    border-radius: 10px;
    background: #356b57;
    color: #fff;
    font-family: "Manrope", "Helvetica Neue", Arial, sans-serif;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: -0.03em;
  }

  .site-header .logo-copy {
    display: flex;
    flex-direction: column;
    line-height: 1.2;
  }

  .site-header .logo-copy strong {
    color: #1c2923;
    font-size: 0.95rem;
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .site-header .logo-copy small {
    margin-top: 3px;
    color: #6e7a73;
    font-size: 0.61rem;
    font-weight: 650;
  }

  .site-header .main-nav {
    display: flex;
    align-items: center;
    gap: 21px;
    flex-wrap: nowrap;
  }

  .site-header .main-nav a {
    padding: 8px 0;
    border-radius: 0;
    background: transparent;
    color: #2a3731;
    font-size: 0.78rem;
    font-weight: 700;
    text-decoration: none;
    transition: color 0.18s ease;
  }

  .site-header .main-nav a:hover {
    background: transparent;
    color: #2f6f8f;
    transform: none;
  }

  .site-header .main-nav .nav-cta {
    padding: 10px 16px;
    border-radius: 9px;
    background: #356b57;
    color: #fff;
    box-shadow: none;
  }

  .site-header .main-nav .nav-cta:hover {
    background: #2b5847;
    color: #fff;
  }

  @media (max-width: 860px) {
    .site-header .nav-container {
      min-height: auto;
      padding: 13px 0 11px;
      flex-direction: column;
      align-items: stretch;
      gap: 11px;
    }

    .site-header .main-nav {
      width: 100%;
      display: flex;
      gap: 16px;
      overflow-x: auto;
      padding-bottom: 2px;
      scrollbar-width: none;
    }

    .site-header .main-nav::-webkit-scrollbar { display: none; }
    .site-header .main-nav a { flex: 0 0 auto; padding: 4px 0; font-size: 0.75rem; }
    .site-header .main-nav .nav-cta { padding: 6px 11px; border-radius: 7px; }
  }
</style>